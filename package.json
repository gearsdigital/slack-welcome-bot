{
  "name": "slack-welcome-bot",
  "private": true,
  "description": "Release tooling (semantic-release) for the Slack Welcome Bot WordPress plugin.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "semantic-release": "^24.0.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1"
  }
}
